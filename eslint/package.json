{
  "name": "eslint",
  "version": "1.0.0",
  "description": "Testing linting with eslint.",
  "main": "index.js",
  "scripts": {
    "lint": "eslint src",
    "lint:report": "eslint --output-file eslint_report.json --format json src",
    "test": "npm run lint"
  },
  "author": "Xingchen Hong",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^7.15.0"
  }
}
